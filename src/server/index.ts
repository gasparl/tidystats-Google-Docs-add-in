import {
  onOpen,
  openSidebar,
} from './ui';

import { docData, insertPara } from './doc_funs';

// Public functions must be exported as named exports
export {
  onOpen,
  openSidebar,
  docData,
  insertPara
};
